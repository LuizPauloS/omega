spring:
    datasource:
        url: jdbc:hsqldb:mem:.
        username: sa
        password:    
        driver-class-name: org.hsqldb.jdbcDriver
    jpa:
        hibernate:
            ddl-auto: none
        show-sql: true

flyway:
    enable: true
    locations: classpath:db/migration/hsql